export enum Endpoints {
  STAR_WARS = "https://swapi-graphql.netlify.app/.netlify/functions/index",
  ENDPOINT_ANIME = "https://graphql.anilist.co",
  ENDPOINT_SPACEX = "https://spacex-production.up.railway.app"
}

export enum GraphQlStartTypes {
  QUERY = "query",
  MUTATION = "mutation",
  SUBSCRIPTION = "subscription"
}
